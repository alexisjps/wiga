<h1>Wigs#index</h1>
<p>Find me in app/views/wigs/index.html.erb</p>

<div class="card-category d-flex justify-content-around" style="background-images:url(wig.url)">
  <% @wigs.each do |wig| %>
    <div class="wig-text">
      <%= image_tag wig.url %>
      <%= wig.name %><br>
    </div>
  <% end %>
</div>
